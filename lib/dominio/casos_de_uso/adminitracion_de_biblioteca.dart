import '../entidades/libro.dart';

class AdministracionDeBiblioteca {
  registrarEntregaDeLibro(DateTime fecha, Libro libro, Usuario usuario) {
    // TODO: implement registrarEntregaDeLibro
    throw UnimplementedError();
  }

  regstrarDevolucionDeLibro(DateTime fecha, Libro libro, Usuario usuario) {

    throw UnimplementedError();
  }
}
